#include <stdlib.h>

int Get_Max2(int a, int b)
{
    printf("enter para is %d,%d",a,b);
    if (a > b)
    {
        return a;
    }
    else
    {
        return b;
    }
}

int Get_Min2(int a, int b)
{
    printf("enter para is %d,%d",a,b);
    if (a > b)
    {
        return a;
    }
    else
    {
        return b;
    }
}